name: Build APK

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: RÃ©cupÃ©rer le code
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Trouver ou extraire le HTML
        run: |
          HTML=$(find . -not -path './.git/*' -not -path './node_modules/*' \
                        -not -path './android/*' \
                        -name "*.html" 2>/dev/null | head -1)
          if [ -z "$HTML" ]; then
            ZIP=$(find . -not -path './.git/*' -name "*.zip" 2>/dev/null | head -1)
            if [ -n "$ZIP" ]; then
              mkdir -p _zip_tmp
              unzip -q "$ZIP" -d _zip_tmp
              HTML=$(find _zip_tmp -name "*.html" | head -1)
            fi
          fi
          if [ -z "$HTML" ]; then
            echo "ERREUR : aucun fichier HTML"
            exit 1
          fi
          echo "HTML : $HTML"
          echo "HTML_PATH=$HTML" >> $GITHUB_ENV

      - name: Injecter le CSS mobile et crÃ©er le projet Android
        run: |
          python3 - << 'PYEOF'
          import os, shutil, re

          html_src = os.environ['HTML_PATH']

          # â”€â”€ Lire le HTML â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
          with open(html_src, 'r', encoding='utf-8', errors='replace') as f:
              html = f.read()

          # â”€â”€ CSS injectÃ© pour tÃ©lÃ©phone Android â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
          mobile_css = """
          <style id="android-mobile-fix">
          /* ===== Optimisation Android phone ===== */

          /* Menubar plus compacte */
          .menubar {
              min-height: 36px !important;
              height: 36px !important;
              font-size: 12px !important;
              padding: 0 4px !important;
          }
          .menubar .logo img { height: 22px !important; }
          .menubar .logo .wm { font-size: 11px !important; }
          .menubar .logo .ver { font-size: 10px !important; }
          .menubar .menu > button { padding: 4px 8px !important; font-size: 11px !important; }

          /* Transport plus compact */
          .transport {
              min-height: 44px !important;
              height: 44px !important;
              padding: 2px 6px !important;
              gap: 4px !important;
          }
          .transport .tbtn {
              width: 34px !important;
              height: 34px !important;
              font-size: 14px !important;
              padding: 0 !important;
          }
          .transport .tempo, .transport .bpm {
              font-size: 18px !important;
          }
          #status {
              font-size: 10px !important;
              padding: 2px 6px !important;
              min-height: 18px !important;
              line-height: 18px !important;
          }

          /* Onglets plus compacts */
          .tabs {
              min-height: 32px !important;
              height: 32px !important;
              overflow-x: auto !important;
              -webkit-overflow-scrolling: touch !important;
          }
          .tab, .tabs button {
              padding: 4px 10px !important;
              font-size: 11px !important;
              white-space: nowrap !important;
          }

          /* Barre patterns plus compacte */
          .patternbar {
              min-height: 30px !important;
              height: 30px !important;
              padding: 2px 6px !important;
              overflow-x: auto !important;
              -webkit-overflow-scrolling: touch !important;
          }
          .patternbar button, .pchip {
              font-size: 11px !important;
              padding: 3px 8px !important;
              height: 26px !important;
          }

          /* Zone de vue : prend tout l'espace restant */
          .view {
              overflow: auto !important;
              -webkit-overflow-scrolling: touch !important;
          }

          /* Barre de statut bas */
          .statusbar, #statBar, .stat-bar {
              font-size: 10px !important;
              padding: 2px 6px !important;
              min-height: 18px !important;
          }
          </style>
          """

          # Injecter avant </head>
          if '</head>' in html:
              html = html.replace('</head>', mobile_css + '\n</head>', 1)
          else:
              html = mobile_css + html

          # â”€â”€ CrÃ©er les dossiers Android â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
          for p in [
              'android/app/src/main/assets',
              'android/app/src/main/java/com/studiotibo/app',
              'android/app/src/main/res/values',
              'android/gradle/wrapper',
          ]:
              os.makedirs(p, exist_ok=True)

          # â”€â”€ Ã‰crire le HTML modifiÃ© â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
          with open('android/app/src/main/assets/index.html', 'w',
                    encoding='utf-8') as f:
              f.write(html)
          size = os.path.getsize('android/app/src/main/assets/index.html')
          print(f"HTML copie avec CSS mobile : {size//1024} Ko")

          # â”€â”€ Fichiers Gradle â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
          open('android/settings.gradle','w').write(
              'rootProject.name = "StudioTibo"\ninclude ":app"\n')

          open('android/build.gradle','w').write(
              'buildscript {\n'
              '    repositories { google(); mavenCentral() }\n'
              '    dependencies { classpath "com.android.tools.build:gradle:8.2.2" }\n'
              '}\nallprojects { repositories { google(); mavenCentral() } }\n')

          open('android/app/build.gradle','w').write(
              'plugins { id "com.android.application" }\n'
              'android {\n'
              '    namespace "com.studiotibo.app"\n'
              '    compileSdk 34\n'
              '    defaultConfig {\n'
              '        applicationId "com.studiotibo.app"\n'
              '        minSdk 26\n'
              '        targetSdk 34\n'
              '        versionCode 2\n'
              '        versionName "3.8.3"\n'
              '    }\n'
              '    buildTypes { debug { minifyEnabled false } }\n'
              '}\n'
              'dependencies { implementation "androidx.webkit:webkit:1.9.0" }\n')

          open('android/gradle.properties','w').write(
              'org.gradle.jvmargs=-Xmx2048m\n'
              'android.useAndroidX=true\n'
              'android.enableJetifier=true\n')

          open('android/app/src/main/AndroidManifest.xml','w').write(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<manifest xmlns:android="http://schemas.android.com/apk/res/android">\n'
              '    <uses-permission android:name="android.permission.RECORD_AUDIO"/>\n'
              '    <uses-permission android:name="android.permission.INTERNET"/>\n'
              '    <application android:label="Studio Tibo"\n'
              '        android:theme="@style/AppTheme"\n'
              '        android:hardwareAccelerated="true">\n'
              '        <activity android:name=".MainActivity"\n'
              '            android:exported="true"\n'
              '            android:screenOrientation="landscape"\n'
              '            android:configChanges="orientation|screenSize|keyboardHidden">\n'
              '            <intent-filter>\n'
              '                <action android:name="android.intent.action.MAIN"/>\n'
              '                <category android:name="android.intent.category.LAUNCHER"/>\n'
              '            </intent-filter>\n'
              '        </activity>\n'
              '    </application>\n'
              '</manifest>\n')

          open('android/app/src/main/java/com/studiotibo/app/MainActivity.java','w').write(
              'package com.studiotibo.app;\n'
              'import android.app.Activity;\nimport android.os.Bundle;\n'
              'import android.view.View;\n'
              'import android.webkit.WebSettings;\nimport android.webkit.WebView;\n'
              'import android.webkit.WebViewClient;\nimport android.webkit.WebChromeClient;\n'
              'public class MainActivity extends Activity {\n'
              '    @Override protected void onCreate(Bundle s) {\n'
              '        super.onCreate(s);\n'
              '        WebView wv = new WebView(this);\n'
              '        setContentView(wv);\n'
              '        WebSettings ws = wv.getSettings();\n'
              '        ws.setJavaScriptEnabled(true);\n'
              '        ws.setDomStorageEnabled(true);\n'
              '        ws.setMediaPlaybackRequiresUserGesture(false);\n'
              '        ws.setUseWideViewPort(true);\n'
              '        ws.setLoadWithOverviewMode(true);\n'
              '        ws.setSupportZoom(true);\n'
              '        ws.setBuiltInZoomControls(true);\n'
              '        ws.setDisplayZoomControls(false);\n'
              '        wv.setScrollBarStyle(View.SCROLLBARS_INSIDE_OVERLAY);\n'
              '        wv.setHorizontalScrollBarEnabled(true);\n'
              '        wv.setVerticalScrollBarEnabled(true);\n'
              '        wv.setWebViewClient(new WebViewClient());\n'
              '        wv.setWebChromeClient(new WebChromeClient());\n'
              '        wv.loadUrl("file:///android_asset/index.html");\n'
              '    }\n}\n')

          open('android/app/src/main/res/values/styles.xml','w').write(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<resources>\n'
              '    <style name="AppTheme" parent="android:Theme.NoTitleBar.Fullscreen"/>\n'
              '</resources>\n')

          open('android/gradle/wrapper/gradle-wrapper.properties','w').write(
              'distributionBase=GRADLE_USER_HOME\ndistributionPath=wrapper/dists\n'
              'distributionUrl=https\\://services.gradle.org/distributions/gradle-8.7-bin.zip\n'
              'zipStoreBase=GRADLE_USER_HOME\nzipStorePath=wrapper/dists\n')

          print("Projet Android pret !")
          PYEOF

      - name: Installer Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Compiler l'APK
        working-directory: android
        run: gradle assembleDebug --no-daemon

      - name: Extraire l'APK
        run: |
          mkdir -p out
          APK=$(find android/app/build/outputs/apk -name "*.apk" | head -1)
          echo "APK : $APK  ($(du -h "$APK" | cut -f1))"
          cp "$APK" out/studio-tibo.apk

      - name: Publier l'APK
        uses: actions/upload-artifact@v4
        with:
          name: studio-tibo-apk
          path: out/studio-tibo.apk
          retention-days: 30

      - name: Release (si tag vX.Y.Z)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: out/studio-tibo.apk
          name: STUDIO TIBO ${{ github.ref_name }}
          body: TÃ©lÃ©chargez studio-tibo.apk sur votre tÃ©lÃ©phone Android.
